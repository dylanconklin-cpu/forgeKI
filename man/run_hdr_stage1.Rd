% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage1_locus.R
\name{run_hdr_stage1}
\alias{run_hdr_stage1}
\title{Run Stage 1 locus and stop-codon resolution}
\usage{
run_hdr_stage1(
  cfg,
  resources,
  transcript_id = cfg$transcript_id,
  scan_bp = 150L
)
}
\arguments{
\item{cfg}{An \code{hdr_config} object.}

\item{resources}{A Stage 1 resource list. For mock/simple tests, \code{genome} is
a named character vector of chromosome/contig sequences. For hg38 runs, use
\code{get_hdr_stage1_hg38_resources()} to create a lazy Bioconductor-backed
genome resource. \code{transcripts} must be a data frame with one row per
transcript and columns \code{gene}, \code{transcript_id}, \code{seqname}, \code{strand}, and
\code{cds_ranges}. \code{cds_ranges} must be a list-column whose entries are data
frames with integer \code{start} and \code{end} columns in 1-based closed genomic
coordinates.}

\item{transcript_id}{Optional transcript override. Defaults to
\code{cfg$transcript_id}.}

\item{scan_bp}{Number of oriented bases to scan beyond the terminal CDS when
the terminal CDS does not already include a stop codon.}
}
\value{
A classed \code{hdr_stage1_result} list containing the selected locus,
transcript audit, terminal equivalence audit, and insertion geometry.
}
\description{
Resolves the target gene, chooses a transcript, collapses CDS sequence in
transcript orientation, validates or discovers the terminal stop codon, and
returns insertion-site geometry for a C-terminal HDR knock-in. Version 0.0.1 supports explicit mock/simple resources and an optional hg38/Bioconductor adapter created by \code{get_hdr_stage1_hg38_resources()}.
}
