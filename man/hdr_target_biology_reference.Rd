% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target_biology_reference.R
\name{hdr_target_biology_reference_schema}
\alias{hdr_target_biology_reference_schema}
\alias{hdr_target_biology_default_reference_path}
\alias{hdr_load_target_biology_reference}
\alias{hdr_build_target_biology_reference}
\alias{hdr_build_target_biology_proteome_reference}
\alias{hdr_fetch_uniprot_target_features}
\alias{hdr_parse_uniprot_features}
\title{Build and consume target-biology reference tables}
\usage{
hdr_target_biology_reference_schema()

hdr_target_biology_default_reference_path()

hdr_load_target_biology_reference(path = NULL)

hdr_build_target_biology_reference(
  genes,
  output_dir = NULL,
  source_mode = c("offline", "uniprot_rest"),
  uniprot_records = NULL,
  uniprot_features = NULL,
  include_curated = TRUE,
  tax_id = 9606L,
  reviewed = TRUE,
  overwrite = TRUE
)

hdr_build_target_biology_proteome_reference(
  output_dir,
  source_mode = c("features_file", "records_file", "uniprot_stream"),
  input_path = NULL,
  tax_id = 9606L,
  reviewed = TRUE,
  include_curated = TRUE,
  max_records = Inf,
  overwrite = TRUE
)

hdr_fetch_uniprot_target_features(
  genes,
  tax_id = 9606L,
  reviewed = TRUE,
  size = 3L
)

hdr_parse_uniprot_features(records)
}
\arguments{
\item{path}{Path to a CSV/RDS target-biology reference table, a data frame
already in memory, or \code{NULL} for an empty reference.}

\item{genes}{Character vector of gene symbols to include.}

\item{output_dir}{Optional directory where CSV, RDS, and manifest files are
written.}

\item{source_mode}{Evidence source mode. \code{offline} never uses the network.
\code{uniprot_rest} fetches UniProt records through the public REST API.}

\item{uniprot_records}{Optional UniProt REST JSON/list records to parse.}

\item{uniprot_features}{Optional pre-flattened UniProt feature table.}

\item{include_curated}{Whether package-curated rules should be included.}

\item{tax_id}{Organism taxonomy identifier.}

\item{reviewed}{Whether REST fetches should prefer reviewed UniProt entries.}

\item{overwrite}{Whether existing output files may be replaced.}

\item{input_path}{Path to a flattened UniProt feature table or saved UniProt
records for file-backed proteome builds.}

\item{max_records}{Optional cap for development/probe proteome builds.}

\item{size}{Maximum records per gene.}

\item{records}{UniProt REST records, a REST search result containing
\code{results}, or an already flattened feature data frame.}
}
\value{
\code{hdr_target_biology_reference_schema()} and
\code{hdr_load_target_biology_reference()} return normalized tibbles.
\code{hdr_build_target_biology_reference()} returns a classed list containing
the reference, build manifest, and output paths. \code{hdr_build_target_biology_proteome_reference()}
returns a classed list containing the slim proteome reference, flattened
features, manifest, and output paths. \code{hdr_fetch_uniprot_target_features()}
returns parsed UniProt REST records. \code{hdr_parse_uniprot_features()} returns
a flattened UniProt feature tibble.
}
\description{
Helpers for building offline-first target-biology references. Normal forgeKI
runs consume saved reference tables or in-memory references; UniProt fetching is
an opt-in refresh operation and is not used during routine pipeline execution.
}
