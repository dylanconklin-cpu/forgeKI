% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_api.R
\name{run_hdr_pipeline}
\alias{run_hdr_pipeline}
\title{Run the HDR design pipeline}
\usage{
run_hdr_pipeline(
  cfg,
  resources = NULL,
  cellline_reference = NULL,
  gene_context_reference = NULL,
  job = NULL,
  job_root = file.path(cfg$output_dir, "jobs"),
  offtarget_mode = c("none", "auto", "exact_genome", "exact_hg38"),
  stage10_mode = c("auto", "skip", "require"),
  guide_scope = c("top_n", "all"),
  top_n = cfg$guide$top_n \%||\% 25L,
  blocking_top_n = NULL,
  write_outputs = TRUE,
  save_rds = cfg$runtime$save_rds \%||\% TRUE,
  render_user_outputs = FALSE,
  user_output_dir = NULL,
  ...
)
}
\arguments{
\item{cfg}{HDR configuration object.}

\item{resources}{Optional Stage 1/2/4 resource object. If \code{NULL}, hg38
Bioconductor resources are loaded with \code{get_hdr_stage1_hg38_resources()}.}

\item{cellline_reference}{Optional fixed global cell-line reference for Stage 10. May be a data frame,
a file path, or a manifest-backed bundle path. If omitted, the orchestrator uses
\code{cfg$stage10$cellline_reference_path} when set, then \code{FORGEKI_CELLLINE_REFERENCE},
then legacy \code{HDRDESIGNR_CELLLINE_REFERENCE} when set.}

\item{gene_context_reference}{Optional v51.2-style gene-wise Stage 10A-10E reference.
If omitted, the orchestrator uses \code{cfg$stage10$gene_context_reference_path} when set,
then \code{FORGEKI_GENE_CONTEXT_REFERENCE}, then legacy \code{HDRDESIGNR_GENE_CONTEXT_REFERENCE} when set.
For MMEJ runs, \code{cfg$stage10$mmej_cellline_reference_path} or \code{FORGEKI_MMEJ_CELLLINE_REFERENCE}
can trigger the method-specific Stage 10A global MMEJ competency branch.
When \code{cfg$stage10$omics_bundle_path} is supplied and \code{build_stage10_reference} is
TRUE, the orchestrator can build a feature-informed Stage 10A-10E reference
from the omics bundle after Stage 9 and attach it as \code{stage10_reference_builder}.}

\item{job}{Optional existing HDR job object returned by \code{new_hdr_job()}.}

\item{job_root}{Root directory for a new job if \code{job} is not supplied.}

\item{offtarget_mode}{Stage 3 off-target mode: \code{none}, \code{auto}, \code{exact_genome}, or \code{exact_hg38}.}

\item{stage10_mode}{Whether Stage 10 should run automatically, be skipped, or be required.}

\item{guide_scope}{Guide subset used for Stage 3 and Stage 6.}

\item{top_n}{Number of top guides/designs to carry through scoped stages.}

\item{blocking_top_n}{Optional narrower number of ranked guides used for HDR
donor-arm recleavage blocking. When \code{NULL}, user-facing HDR render runs
default to the first five Stage 2 guides so unexported long-tail candidates do
not introduce unexplained donor-arm edits; non-rendered runs retain
\code{top_n}.}

\item{write_outputs}{Whether stage tables should be written to disk.}

\item{save_rds}{Whether stage objects should be saved as RDS files.}

\item{render_user_outputs}{Whether to render the user-facing report bundle
after the pipeline completes.}

\item{user_output_dir}{Optional directory for the user-facing report bundle.}

\item{...}{Reserved for future API-compatible arguments.}
}
\value{
A classed \code{hdr_result} list containing the job, all completed stage
objects, stage output paths, and a compact status summary.
}
\description{
Executes the currently gated HDR stages in a controlled local job directory while
preserving the ability to call each stage independently. The orchestrator writes
per-stage RDS files and table outputs under the job output directory. In v0.0.1
the orchestrator is intentionally conservative: hg38 resources must be available
or supplied explicitly, genome-wide off-target scanning is not attempted unless
requested, and Stage 10 runs only when a fixed reference or an internal omics
bundle is supplied or required by configuration.
}
