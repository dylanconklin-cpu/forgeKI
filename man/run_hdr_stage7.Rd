% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage7_virtual_allele.R
\name{run_hdr_stage7}
\alias{run_hdr_stage7}
\title{Run Stage 7 virtual edited-allele validation}
\usage{
run_hdr_stage7(
  cfg,
  stage1_result,
  stage4_result = NULL,
  stage5_result = NULL,
  stage6_result = NULL,
  cassette_sequence = NULL,
  cassette_path = NULL,
  append_stop_if_missing = TRUE,
  default_stop_codon = "TAA",
  typeiis_enzymes = hdr_stage_typeiis_enzymes(cfg)
)
}
\arguments{
\item{cfg}{An \code{hdr_config} object.}

\item{stage1_result}{A \code{hdr_stage1_result} returned by \code{run_hdr_stage1()}.}

\item{stage4_result}{Optional \code{hdr_stage4_result} used when later arm stages
are not supplied.}

\item{stage5_result}{Optional \code{hdr_stage5_result} containing domesticated arms.}

\item{stage6_result}{Optional \code{hdr_stage6_result} containing blocking arms.}

\item{cassette_sequence}{Optional cassette DNA sequence. When omitted, Stage 7
attempts to resolve a cassette FASTA from \verb{inst/extdata/cassettes/<cassette_id>/}.}

\item{cassette_path}{Optional FASTA file containing the cassette sequence.}

\item{append_stop_if_missing}{Whether to append \code{default_stop_codon} when the
cassette lacks a terminal stop codon.}

\item{default_stop_codon}{Stop codon appended when required.}

\item{typeiis_enzymes}{Character vector of Type IIS enzymes to audit in the
final donor payload.}
}
\value{
A classed \code{hdr_stage7_result} with cassette QC, virtual-allele QC,
junction QC, donor-payload sequence, and residual Type IIS audit hits.
}
\description{
Builds and validates an in-silico edited coding sequence and donor payload
model. Stage 7 prefers Stage 6 blocking arms, then Stage 5 domesticated arms,
then Stage 4 raw arms. The cassette may be supplied directly as a DNA sequence,
as a FASTA path, or resolved from the package toy cassette library.
}
