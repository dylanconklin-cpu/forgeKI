% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{hdr_config}
\alias{hdr_config}
\title{Create an HDR design configuration object}
\usage{
hdr_config(
  gene,
  project_dir,
  method = c("hdr", "mmej"),
  cassette_id = "HiBiT_dTAG_GFP_EF1a_BSD_P2A_mKATE",
  donor = NULL,
  resources = file.path(project_dir, "hdr_resources.yml"),
  transcript_id = NULL,
  genome_build = "hg38",
  organism = "human",
  output_dir = file.path(project_dir, "runs", safe_file_stub(gene)),
  execution_mode = c("local", "private_server", "public_server"),
  output_profile = c("full_internal", "user_facing", "collaborator", "public"),
  guide = hdr_guide_options(),
  arms = hdr_arm_options(),
  mmej = hdr_mmej_options(),
  golden_gate = NULL,
  stage10 = hdr_stage10_options(),
  crisprverse = hdr_crisprverse_options(),
  biology = hdr_biology_options(),
  runtime = hdr_runtime_options()
)
}
\arguments{
\item{gene}{Gene symbol, such as \code{ACTB} or \code{TIPARP}.}

\item{project_dir}{Project directory used to resolve manifests and local outputs.}

\item{method}{Repair pathway. \code{"hdr"} uses the existing long-homology-arm HDR
workflow. \code{"mmej"} enables staged PITCh/MMEJ integration.}

\item{cassette_id}{Deprecated legacy cassette/payload identifier used by Stage 7 sequence simulation until full module-sequence migration. New user-facing code should select \code{donor} modules instead.}

\item{donor}{Donor module options from \code{forgeki_donor_options()} / \code{hdr_donor_options()}.}

\item{resources}{Path to an HDR resource manifest YAML/JSON file.}

\item{transcript_id}{Optional transcript identifier override.}

\item{genome_build}{Genome build. Version 0.0.1 supports \code{hg38}.}

\item{organism}{Organism label. Version 0.0.1 supports \code{human} infrastructure only.}

\item{output_dir}{Output directory for local runs.}

\item{execution_mode}{Execution mode: \code{local}, \code{private_server}, or \code{public_server}.}

\item{output_profile}{Output redaction/profile mode: \code{full_internal},
\code{user_facing}, \code{collaborator}, or \code{public}.}

\item{guide}{Guide options from \code{hdr_guide_options()}.}

\item{arms}{Homology-arm options from \code{hdr_arm_options()}.}

\item{mmej}{MMEJ/PITCh options from \code{hdr_mmej_options()}. Used when
\code{method = "mmej"}.}

\item{golden_gate}{Golden Gate donor options from \code{hdr_golden_gate_options()}. These are synchronized with \code{donor} metadata when possible.}

\item{stage10}{Stage 10/cell-line context options from \code{hdr_stage10_options()}.}

\item{crisprverse}{Optional crisprVerse integration options from
\code{hdr_crisprverse_options()}.}

\item{biology}{Target-biology review options from \code{hdr_biology_options()}.}

\item{runtime}{Runtime options from \code{hdr_runtime_options()}.}
}
\value{
A validated object of class \code{hdr_config}.
}
\description{
Version 0.0.1 creates and validates configuration only. It does not execute design stages.
}
