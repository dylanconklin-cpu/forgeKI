% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage1_bioc_adapter.R
\name{forgeki_install_hg38_resources}
\alias{forgeki_install_hg38_resources}
\title{Install hg38 Bioconductor resources used by forgeKI}
\usage{
forgeki_install_hg38_resources(
  packages = forgeki_missing_hg38_packages(),
  ask = interactive(),
  update = FALSE,
  install_biocmanager = TRUE
)
}
\arguments{
\item{packages}{Character vector of package names to install. Defaults to the
missing packages from \code{forgeki_hg38_bioc_packages()}.}

\item{ask}{Passed to \code{BiocManager::install()}. Defaults to \code{interactive()}.}

\item{update}{Passed to \code{BiocManager::install()}. Defaults to \code{FALSE} so the
helper installs missing resources without updating the user's full library.}

\item{install_biocmanager}{If \code{TRUE}, install \code{BiocManager} from CRAN when it
is not already available.}
}
\value{
Invisibly returns the package vector that was requested.
}
\description{
Installs the Bioconductor packages required for the default exact hg38
pipeline resources. This is intentionally a user-called setup step rather
than an automatic package-install hook because the genome and annotation
resources are large and come from Bioconductor.
}
