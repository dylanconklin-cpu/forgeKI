% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job_model.R
\name{new_hdr_job}
\alias{new_hdr_job}
\title{Create an isolated HDR job directory}
\usage{
new_hdr_job(
  root_dir = NULL,
  cfg = NULL,
  prefix = format(Sys.time(), "\%Y\%m\%d_\%H\%M\%S")
)
}
\arguments{
\item{root_dir}{Root directory for job folders, or an \code{hdr_config} object when using \code{new_hdr_job(cfg)}.}

\item{cfg}{Optional HDR configuration object.}

\item{prefix}{Job-id prefix. Defaults to a timestamp.}
}
\value{
A list containing the job id and normalized job subdirectories.
}
\description{
\code{new_hdr_job()} accepts either \code{new_hdr_job(cfg)} or \code{new_hdr_job(root_dir, cfg)}.
When \code{root_dir} is omitted, jobs are written under \code{file.path(cfg$output_dir, "jobs")}.
}
