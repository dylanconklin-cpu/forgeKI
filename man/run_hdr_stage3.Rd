% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3_guide_risk.R
\name{run_hdr_stage3}
\alias{run_hdr_stage3}
\title{Run Stage 3 guide off-target and guide-risk annotation}
\usage{
run_hdr_stage3(
  cfg,
  stage2_result,
  resources = NULL,
  stage6_result = NULL,
  stage8_result = NULL,
  offtarget_mode = c("auto", "none", "exact_genome", "exact_hg38"),
  guide_scope = c("all", "top_n"),
  top_n = cfg$guide$top_n \%||\% 25L
)
}
\arguments{
\item{cfg}{An \code{hdr_config} object.}

\item{stage2_result}{A \code{hdr_stage2_result} returned by \code{run_hdr_stage2()}.}

\item{resources}{Optional resources containing \code{genome}. Named character
genomes are scanned exactly. Lazy Bioconductor-backed hg38 genomes are scanned only when
\code{offtarget_mode = "exact_hg38"}.}

\item{stage6_result}{Optional \code{hdr_stage6_result} with recleavage-protection
audit status.}

\item{stage8_result}{Optional \code{hdr_stage8_result} with donor-orderability QC.}

\item{offtarget_mode}{One of \code{auto}, \code{none}, \code{exact_genome}, or \code{exact_hg38}.}

\item{guide_scope}{One of \code{all} or \code{top_n}.}

\item{top_n}{Number of Stage 2-ranked guides to annotate when
\code{guide_scope = "top_n"}.}
}
\value{
A classed \code{hdr_stage3_result} list containing guide-risk annotations,
exact off-target hit records, and a compact QC summary.
}
\description{
Annotates Stage 2 guide candidates with exact-match off-target counts when a
simple in-memory genome is supplied, or conservative exact hg38 target counts
when a lazy Bioconductor genome resource is supplied and \code{offtarget_mode = "exact_hg38"}. It then combines guide-practicality, recleavage-protection,
and donor-orderability context into a guide-risk table. This stage does not
perform mismatch-tolerant genome-wide off-target scoring; that heavier analysis
should be added later as a separate backend.
}
